Para poder ejecutar necesita instalar node js

Una vez instalado node js debe instalar las librerias con
**npm install**

Luego de instalar para ejecutar el programa ejecute el siguiente codigo en su consola
**npm start**
